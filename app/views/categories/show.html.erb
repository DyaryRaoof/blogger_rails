<h1 class="text-center m-2">Category <%= @category.name %></h1>
 <% if logged_in? &&  current_user.admin? %>
  <div class="d-flex justify-content-center mt-3">
    <%= link_to "Edit Category Name", edit_category_path(@category.id), class: "btn btn-outline-info"%>
  </div>
<%end%>

<h3 class="text-center m-2">Articles</h3>
<%= render "shared/pagination", obj: @articles%>
<%= render @articles%>
<%= render "shared/pagination", obj: @articles%>
